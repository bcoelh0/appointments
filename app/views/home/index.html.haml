.container
  .row
    .col-sm-8.offset-sm-2.text-center.mt-5
      %h1 Welcome to appointments!

      .mt-5
        - if user_signed_in?
          = link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-warning"
        - else
          = link_to "Register", new_user_registration_path, class: "btn btn-info"
          = link_to "Login", new_user_session_path, class: "btn btn-success"
